window.onload = () => {
    const person = { name:'Henry', age: 32 }
    const { name, age } = person

    // Array destructuring
    const arr = [1, 2 ,3]
    const [a, b, c] = arr
    console.log(a);
    console.log(b);
    console.log(a);

    const []
}